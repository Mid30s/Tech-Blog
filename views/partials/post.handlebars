<div class="card mb-4">
    <div class="card-header">
        <h3>{{title}}</h3>
    </div>
    <div class="card-body">
        <p>{{content}}</p>
        <br>
        <p><small>Posted by {{user.name}} on {{format_date createdAt}}</small></p>
    </div>
</div>
